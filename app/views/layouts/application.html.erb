<!DOCTYPE html>
<html>
<head>
  <title><%= t('content.reports.titles.head') %></title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

  <nav class="navbar navbar-expand-lg navbar-light" style="background-color: #333333;">
    <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <a class="navbar-brand text-white" href="/"><%= t('content.reports.titles.main') %></a>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav mr-auto">
        <a href="/" class="nav-item nav-link text-white" ><%= t('content.menu.strikers') %></a>
        <a href="/reports/matches" class="nav-item nav-link text-white"><%= t('content.menu.matches') %></a>
        <a href="/reports/players" class="nav-item nav-link text-white"><%= t('content.menu.players') %></a>
        <% if user_signed_in? %>
          <a class="navbar-brand text-white brand-admin" href="#"> &nbsp;&nbsp;&nbsp;|</a>
          <a href="/matches" class="nav-item nav-link text-admin"><%= t('content.menu.matches') %></a>
          <a href="/seasons" class="nav-item nav-link text-admin"><%= t('content.menu.seasons') %></a>
          <a href="/videos" class="nav-item nav-link text-admin"><%= t('content.menu.videos') %></a>
          <a href="/presences" class="nav-item nav-link text-admin"><%= t('content.menu.report_goals') %></a>
          <a href="/players" class="nav-item nav-link text-admin"><%= t('content.menu.players') %></a>
        <% end %>
       </div>
       <div class="logout">
         <% if user_signed_in? %>
           <%= link_to "Logout", destroy_user_session_path, :method => :delete, class: "btn btn-blue" %>
         <% else %>
           <%= link_to t('devise.links.sign_in'), new_user_session_path, class: "btn btn-blue" %>
         <% end %>
       </div>
      </div>
    </div>
  </nav>

  <div class="row">
    <div class="container-fluid">
      <% flash.each do |key, message| %>
        <div class="text-center alert alert-<%= key == 'notice' ? 'success' : 'danger' %>">
          <%= message %>
        </div>
      <% end %>
    </div>
  </div>

  <div class="container">
    <div class="row">
      <%= yield %>
    </div>
  </div>

</body>
</html>
